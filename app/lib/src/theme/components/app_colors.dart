import 'package:flutter/material.dart';

class AppColors {
  // ORIGINAL COLORS (for existing screens)
  static const saffron = Color(0xFFFF9933);
  static const saffronLight = Color(0xFFFFB866);
  static const saffronDark = Color(0xFFCC7A29);

  static const gold = Color(0xFFC8A951);
  static const white = Colors.white;
  static const bg = Color(0xFFF7F2EC);

  // NEW THEME-D COLORS (for Landing, Services, etc)
  static const backgroundLight = Color(0xFFF5F5F5);
  static const textDark = Color(0xFF1A1A1A);
  static const primaryGold = Color(0xFFC18F4A);
}
